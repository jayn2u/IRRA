WANDB_API_KEY=your-wandb-api-key
WANDB_ENTITY=your-wandb-entity
WANDB_PROJECT=irra

# Root directory holding CUHK-PEDES/, ICFG-PEDES/, RSTPReid/. Differs per
# machine (e.g. /mnt/data/lab_datasets vs /data/jayn2u/lab_datasets); set this
# in your own env/.env (gitignored) rather than editing scripts or code.
DATASET_ROOT_DIR=/path/to/lab_datasets
